# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/)
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

Generated by [`auto-changelog`](https://github.com/CookPete/auto-changelog).

* * *

{{#each releases}}
  {{#if href}}
    ## Release [{{title}}]({{href}}){{#if tag}} ({{isoDate}}){{/if}}
  {{else}}
    ## Release {{title}}{{#if tag}} ({{isoDate}}){{/if}}
  {{/if}}

  {{#if summary}}
    {{summary}}
  {{/if}}

  {{! List commits with `Breaking change: ` somewhere in the message }}
  {{#commit-list commits heading='### 💥 Breaking Changes' message='(BREAKING CHANGE: |Breaking change: )'}}
    - {{subject}} [`{{shorthash}}`]({{href}})
  {{/commit-list}}

  {{! List commits that add new features, but not those already listed above }}
  {{#commit-list commits heading='### 🚀 New Features' message='feat: ' exclude='(BREAKING CHANGE: |Breaking change: )'}}
    - {{subject}} [`{{shorthash}}`]({{href}})
  {{/commit-list}}
  
  {{! List commits that add docs, but not those already listed above }}
  {{#commit-list commits heading='### 🐛 Bug fix' message='fix: ' exclude='(BREAKING CHANGE: |Breaking change: )'}}
    - {{subject}} [`{{shorthash}}`]({{href}})
  {{/commit-list}}

  {{#if merges}}
    ### Merged

    {{#each merges}}
      - {{message}} {{#if href}}[`#{{id}}`]({{href}}){{/if}}
    {{/each}}
  {{/if}}
{{/each}}
